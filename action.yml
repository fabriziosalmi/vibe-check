name: 'VibeGuard Auditor'
description: 'Anti-slop CI/CD gatekeeper v1.4.0 - Modular architecture with AST parsing, inline ignores, and 115 configurable rules.'
author: 'fabriziosalmi'
branding:
  icon: 'shield'
  color: 'purple'

inputs:
  threshold:
    description: 'Minimum score required to pass the pipeline (0-1000)'
    required: false
    default: '800'
  brutal_mode:
    description: 'Enable brutal mode: double penalties and fail-fast on critical security violations'
    required: false
    default: 'false'

outputs:
  score:
    description: 'The final calculated score after all deductions'

runs:
  using: 'docker'
  image: 'Dockerfile'
